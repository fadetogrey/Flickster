<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="OverviewFont">
        <item name="android:textSize">@dimen/overview_text_size</item>
    </style>

    <style name="CustomTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/darkThemeBackgroundColor</item>
        <item name="android:textColor">@color/darkThemeForegroundColor</item>
    </style>

    <style name="DetailsTheme" parent="Theme.AppCompat">
        <item name="android:background">@color/detailsBackgroundColor</item>
    </style>

    <style name="RatingBarTheme" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/ratingBackgroundColor</item>
        <item name="colorControlActivated">@color/ratingBackgroundColor</item>
    </style>
</resources>
